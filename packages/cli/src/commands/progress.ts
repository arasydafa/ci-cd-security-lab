import chalk from 'chalk';

export function progressCommand(): void {
  console.log(chalk.bold('\n  CI/CD Security Lab — Progress\n'));
  console.log(chalk.dim('  Progress tracking will be available after Phase 2.'));
  console.log(chalk.dim('  Complete challenges using the CLI to build your skills.\n'));
}
